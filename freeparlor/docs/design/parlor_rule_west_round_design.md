# 西入（サドンデス）の扱い — フリー雀荘ルールとの不一致

**日付:** 2026-07-31
**ステータス:** **裁定済み（2026-07-31、Gamba）= W1 + S1。実装は L1 O1 の完走後に着手する。**
本書 §4/§5 の選択肢のうち **W1（config フラグ化・既定は現状のまま = ビット不変）** と
**S1（O1 を現行ルールで先に走らせ、その後に W1 を実装して以後の run を西入なしへ）** が採択された。
**現時点では実装していない**（S1 の順序を守るため）
**起票:** 2026-07-30 のレンズ4 の副産物（`../reports/qualitative_review_anchor_k_b04_20260731.md` §6）。
Gamba「**一般的に西入は雀荘ルールでは採用されません。結構レアケースではありますが、
学習にある程度影響を与える可能性があるので消したいですね**」
**計測器:** `freeparlor/scripts/analyze_west_round_impact.py`（新規・read-only）

---

## 1. 現状の実装（一次ソース）

`libriichi/src/arena/game.rs:74-98`（`Game::poll` の局開始判定）:

```rust
// after W4
// or, after all-last
//   and, oya is not in renchan
//   and, anyone has more than 30k
if self.kyoku >= self.length + 4
    || self.kyoku >= self.length
        && !self.in_renchan
        && self.scores.iter().any(|&s| s >= 30000)
{
    self.ended = true;
```

- `length = 8`（東風なら 4）。半荘は `kyoku` 0..7 が東1〜南4
- **南4終了時に 30000 点以上の者が居なければ終わらず、`kyoku = 8` に進む**。
  場風は `bakaze = E + kyoku / 4`（`board.rs:245`）なので `kyoku 8..11` = **西1〜西4**
- 打ち切りは `kyoku >= length + 4` = **西4終了**
- 親の連荘は別条件（`game.rs:160-178`）: オーラスで親が 30000 点以上**かつトップ**なら終了、
  それ以外は連荘（`honba += 1`、`kyoku` は進まない）
- 天鳳ルール準拠であることはコード中のコメントが明示している
  （`game.rs:57-64` が天鳳のサドンデス規定を引用）

**フリー雀荘ルールでは通常、南4終了で 30000 点未満でもそのまま終局**する（西入なし）。

## 2. 影響の実測（判定と同一の 1v3 牌譜 n=800・両脚）

`analyze_west_round_impact.py`。西1局の開始直前でログを打ち切った**反実仮想**
（= 西入なしルールでの終局）を作り、challenger の3ストリームを実際の結果と比較した。
経済ロジックは `analyze_freeparlor_pnl_1v3.py` / `chip_from_log.py` をそのまま呼ぶ（複製禁止）。

| | init 脚 | b04-16000 脚 |
|---|---:|---:|
| **西入した半荘** | 27/800（**3.38%**） | 29/800（**3.62%**） |
| 西場の局数（西入1回あたり） | 59（2.19 局） | 45（1.55 局） |
| 西入した半荘のうち**順位点が変わった** | 20/27（74.1%） | 14/29（48.3%） |
| → 全半荘に対して | **2.50%** | **1.75%** |

challenger 視点の「西入あり − 西入なし」（西入した半荘のみの平均 / 全 800 半荘に均した寄与）:

| ストリーム | init | b04 |
|---|---:|---:|
| 素点（千点） | −0.081 / **−0.0028** | −0.586 / **−0.0212** |
| 順位点 | −0.370 / **−0.0125** | −3.103 / **−0.1125** |
| チップ（枚） | −0.037 / **−0.0013** | −0.138 / **−0.0050** |
| 合算 | −0.637 / **−0.0215** | −4.379 / **−0.1588** |

### 2a. 判定への影響（現行の凍結済み判定はどれも動かない）

| 判定/参考値 | 実測の効果量 | 西入の寄与（b04 − init の差分） | 比 |
|---|---:|---:|---|
| 判定2 チップ | +0.405 枚（SE 0.231） | 約 −0.004 枚 | **効果量の 1%・SE の 2%** |
| 素点（参考） | +0.492（SE 0.762） | 約 −0.018 | SE の 2% |
| 順位点（参考） | +0.912（SE 1.187） | 約 **−0.100** | **SE の 8%** |

**⇒ 西入を消しても C/K/b04 の判定は変わらない**（最大でも順位点で 1SE の 8%）。
放銃率・和了率は局単位の指標なので、西場の局が消えても率としてはほぼ動かない。

### 2b. 測れていないこと（過小評価になりうる）

**上表は事後集計であり、方策の意思決定の変化を含まない。**
西入があると「オーラスで 30000 点に届かせる」ことに価値があり、
無ければオーラスは純粋に着順勝負になる。**オーラスの押し引き・打点選択が変わる**ため、
学習への実効はここで測った量より大きくなりうる。**方向は不明**（測るには実際にルールを
変えて run を回すしかない）。

## 3. 変更の含意（ここが裁定の本体）

1. **これは環境（報酬を生む世界）の変更であり、新しい実験レジームになる。**
   変更後に測った値は、変更前に**訓練された**方策の登録値と厳密には比較できない
   （eval だけなら同一レジームで両脚を測れるので内部妥当性は保たれる）
2. **凍結中の事前登録との衝突**: `l1_o1_submit_every_design.md` は
   **plain PPO を baseline とし、参照脚に Stage1-16000 を使う**ことを凍結した（§11）。
   Stage1-16000 は**西入ありで訓練された** checkpoint なので、
   **O1 を西入なしで訓練すると「submit_every」と「ルール変更」の2変数になる**
3. **libriichi の Rust 変更**（`CLAUDE.md`「新規 Rust 表面積は最小に」）。
   ビルド手順と import スモークが必須。`arena` 側の変更で、`state`（プレイヤー視点）や
   obs には触らないので**観測空間・ネットワークは不変**
4. **本家との分岐**: 天鳳ルール準拠から離れる。`enable_rule_based_agari_guard` のように
   **既定 OFF のフラグ**にすれば既存経路はビット不変に保てる

## 4. 選択肢

| 案 | 内容 | 長所 | 短所 |
|---|---|---|---|
| **W1** | **config フラグ化**（例 `[env] enable_west_round`、**既定 = true = 現状**）。false で南4終了 | 既存経路がビット不変。検定でデフォルト恒等性を assert できる。いつでも A/B できる | Rust + config + 検定の3点セット（半日程度） |
| **W2** | ハード変更（西入を無条件で消す） | 実装最小 | 過去 run の再現ができなくなる。本家挙動に戻せない |
| **W3** | 当面変更しない（負債として記録） | コスト 0 | 商品ルールとの乖離が残る |

**実装エージェントの推奨: W1。** 理由は (a) `p_enrich` / `anchor_prob` / `kl_beta` と同じ
「既定 OFF で既存経路ビット不変」の前例に乗れる、(b) ルール変更の効果自体を
**単一変数実験として測れる**（西入あり/なしの対照）、(c) 商品プリセット
（`product_gaps_design_notes.md` G3 の雀荘ごとのウマオカ・チップ）と同じ枠に収まる。

## 5. 実施順の提案（**裁定事項**）

GPU は1系統で、直近の枠は L1 O1 が押さえている。順序の候補:

| 順 | 案 | 影響 |
|---|---|---|
| **S1（推奨）** | **O1 を現行ルール（西入あり）で先に走らせる** → その後に W1 を実装し、以後の run を西入なしへ | O1 の単一変数が保たれる。ルール変更は L1 O3 以降の regime 切り替え点になる |
| S2 | W1 を先に実装し、O1 から西入なしで走らせる | O1 の baseline（Stage1-16000 = 西入ありで訓練）との比較が2変数になる。参照脚を n=800 で測り直す予定（§7 の凍結事項）なので **eval 側は揃うが訓練側が揃わない** |
| S3 | W1 を実装するが**既定 true のまま**（フラグだけ用意して当面 OFF にしない） | 実装だけ先行して意思決定を後送りできる。O1 への影響ゼロ |

**S1 か S3 なら O1 の事前登録（凍結済み）に一切触らずに済む。**
S2 を採る場合は O1 の凍結記録に amendment（baseline の再定義）が要る。

## 5a. 裁定の記録（2026-07-31、Gamba）

> 「**W1とS1で行きましょう**」

| 項目 | 裁定 |
|---|---|
| 実装方式 | **W1 = config フラグ化。既定は現状（西入あり）でビット不変**。検定でデフォルト恒等性を assert する |
| 実施順 | **S1 = L1 O1 を現行ルール（西入あり）で走らせてから W1 を実装**。以後の run を西入なしへ |
| O1 への影響 | **なし**（凍結済みの `l1_o1_submit_every_design.md` には一切触れない） |

**着手条件: L1 O1 の完走・判定後。** 実装時に決めることは §6-2/§6-3 に残る。

## 6. 未決事項（実装時に決める）

1. ~~W1/W2/W3 の選択と、S1/S2/S3 の実施順~~ → **§5a で裁定済み（W1 + S1）**
2. 西入を消す場合の**細部のルール**（オーラス親の連荘条件をどうするか。
   現行は「親が 30000 点以上かつトップなら終了」= アガリやめ相当。
   雀荘では「トップなら親の連荘を選択できる／できない」が店ごとに違う）
3. 他にも本家（天鳳）ルールと雀荘ルールの差が無いかの棚卸し
   （割れ目・途中流局の採否・ダブロン/トリロン・南入時の供託・飛び終了の点数など）。
   **今回1件見つかった以上、体系的に洗うべき**（レンズ4 の副産物として起票）

---

## 7. W1 の実装と run-validation（2026-08-02）

**ステータス: W1 実装完了・検証済み。** ブランチ `w1-west-round-flag`。

### 7-1. 実装

| 層 | 変更 |
|---|---|
| `libriichi/src/arena/game.rs` | `Game.disable_west_round` / `BatchGame.disable_west_round` を追加。終局条件を `... && (self.disable_west_round \|\| self.scores.iter().any(\|&s\| s >= 30000))` に。**`Game` は `Default` derive なので、既定値 `false` が天鳳準拠になる向き**でフィールドを持つ（`enable_` にすると `..Default::default()` 経由の構築が黙ってルールを変える） |
| `libriichi/src/arena/one_vs_three.rs` | `OneVsThree` に pyo3 kwarg `enable_west_round=true`（既定 = 現行挙動）を追加し `BatchGame` へ渡す |
| `mortal/config.py` | `_env_defaults` に `'enable_west_round': True` |
| 呼び出し側（全5ファイル） | `player.py`(4箇所) / `one_vs_three.py` / `eval_grp_baseline_1v3.py` / `eval_meta_stage1_vs_stage2.py` / `drca_common.py` が config から読む |
| `verify_ppo_p1.py` | **検定(22)** を追加（config 既定 True / pyo3 既定 True / フラグの受理） |

**親の連荘条件（`game.rs:160-178`）は変更していない**（§6-2 は未決のまま）。
`TwoVsTwo` は Python から使われていないので `tenhou_hanchan()` の既定（西入あり）のまま。

### 7-2. run-validation（実機・1v3 n=800・init 対 init）

**(a) 既定はビット不変** — 同一 seed・同一 `EVAL_SEED_COUNT=200` で変更前の
`game_logs_init` と比較:

| 比較 | 結果 |
|---|---|
| イベント列 | **800/800 完全一致** |
| `meta` の `q_values` まで含む（`eval_time_ns` のみ除外） | **800/800 完全一致** |

**(b) フラグ OFF の挙動**:

| 検査 | 結果 |
|---|---|
| 西場の局数 | ON 59 → **OFF 0** |
| 西入しなかった半荘 | **773/800 が完全一致** |
| 西入していた半荘 | **27/27 が「西1 開始の直前まで完全一致 + `end_game` で終局」** |

27/800 = 3.38% は §2 の実測（init 脚 27/800）と一致する。

### 7-3. 副産物の知見（**運用に効く**）

最初の比較を `EVAL_SEED_COUNT=50` で行ったところ、**200 半荘中 12 件（6%）で行動列が食い違った**。
原因は seed_count が変わると **GPU バッチ形状が変わり** `q_values` の下位桁がずれ、
僅差の局面で argmax が反転するため（打牌そのものは同一なのに、以降の局が分岐する）。

⇒ **「eval は決定論」という本プロジェクトの前提は、`EVAL_SEED_COUNT`（バッチ形状）が
同一であることを条件とする。** n=400 の測定と n=800 の測定を「同じもの」として
突き合わせるときはこの点に注意すること（統計的検出力とは別の論点）。

## 8. S1 の実施方法（**裁定事項**）

W1 は入ったので、あとは「以後の run を西入なしにする」だけ。方法が2つある:

| 案 | 内容 | 長所 | 短所 |
|---|---|---|---|
| **S1a（推奨）** | `config.py` の既定は **True のまま**にし、**新しい run の config で明示的に `enable_west_round = false`** を書く | 過去 run の config を再実行しても当時の挙動を再現できる（reproducibility を壊さない） | 新 config ごとに書き忘れの余地 → launcher の凍結値検査で assert すれば潰せる |
| S1b | `config.py` の既定を **false に反転** | 書き忘れが起きない | **過去の全 config の意味が黙って変わる**（「サイレント修正禁止」に抵触） |

### 8a. 裁定の記録（2026-08-02、Gamba）

> 「**S1aでやりましょう**」

| 項目 | 裁定 |
|---|---|
| 既定の扱い | **`config.py` の既定は `True`（天鳳準拠）のまま据え置く**。過去 run の config を再実行しても当時の挙動が再現できる |
| 新 run の扱い | **新しい run の config に `[env] enable_west_round = false` を明示的に書く** |
| 書き忘れの防止 | 次の run の launcher に `enable_west_round = false` の**存在検査**を入れる（L1 O1/O3 の凍結値検査と同じ方式）。書き忘れたら発進しない |

⇒ **S1 は「新レジーム最初の run の設計・発進」とセットで完了する。**
W1（本書 §7）までが独立に完了した状態で、S1 の残作業は §8-1 の再測定と
次 run の config/launcher。

### 8-1. S1 実施時に必要になる再測定

ルールが変わるので、**init と参照脚を新ルールで測り直す**必要がある（eval のみ・GPU 数時間）:

- init 脚（判定1/2 の分母）
- 参照脚 Stage1-16000（`Δ放銃_ref`。現行の凍結値 +2.876pp は**西入あり**での測定）

**訓練側は揃わない**（init も Stage1-16000 も西入ありで訓練された checkpoint）。
これは §3-1 のとおり避けられない。新レジーム最初の run の設計書で留保として明記すること。

